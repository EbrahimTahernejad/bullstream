linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - misspell
    - unconvert
    - unparam

issues:
  exclude-rules:
    # Ignore errors in cmd packages (log.Fatal covers them)
    - path: cmd/
      linters: [errcheck]
